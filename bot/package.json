{
  "name": "nika-net-launcher",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Nika Net Launcher — a Telegram bot on Cloudflare Workers that builds Nika Net panels for you.",
  "scripts": {
    "build": "node scripts/build.js",
    "deploy": "npm run build && node scripts/deploy.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260603.1",
    "esbuild": "^0.28.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.9.0"
  }
}
