# Nika Net — v0.6.1 (رفع قطعی اتصال کانفیگ‌ها)

## مشکل چه بود؟
کانفیگ‌هایی که پنل می‌ساخت وصل نمی‌شدند و پینگ نداشتند. دو دلیل اصلی پیدا و رفع شد:

### ۱) باگ اصلی — احراز هویت اشتباه (403 برای همه)
- ورکر کاربر را از پارامتر `?uuid=...` داخل URL وب‌سوکت می‌خواست و بدون آن `403 "no user for this uuid"` برمی‌گرداند.
- اما ژنراتور کانفیگ هرگز `uuid` را در URL نمی‌گذاشت؛ uuid فقط داخل **هدر VLESS** (و SHA-224 رمز داخل **هدر Trojan**) ارسال می‌شد — مثل edgetunnel استاندارد.
- نتیجه: هر اتصالی قبل از شروع پروکسی رد می‌شد.

**راه‌حل:** شناسایی و احراز هویت کاربر حالا از **داخل خودِ هدر پروتکل** انجام می‌شود:
- VLESS → uuid داخل هدر (۱۶ بایت بعد از version)
- Trojan → SHA-224(password) داخل هدر
- اگر کلاینت `uuid=` در کوئری بفرستد هم همچنان پشتیبانی می‌شود (سازگار با نسخهٔ قبل).

### ۲) IPهای پیش‌فرض خراب
- پیش‌فرض قبلی `["1.0.0.1","104.16.132.229","188.114.96.9"]` بود.
- تست TLS واقعی نشان داد `1.0.0.1` و `104.16.132.229` → **HTTP 403** (این‌ها Edge نیستند؛ 1.0.0.1 ریسولور DNS کلادفلر است).
- فقط `188.114.96.9` → HTTP 200.

**راه‌حل:**
- پیش‌فرض جدید: `188.114.96.9`, `162.159.192.1`, `104.17.147.22`, `172.67.161.1` (همه تست‌شده: HTTP 200).
- **پاک‌کنندهٔ خودکار**: IPهای بد (ریسولورها + IPهای قدیمی 403) که در تنظیمات پنل‌های قدیمی ذخیره شده بودند، هنگام بارگذاری حذف و با IP سالم جایگزین می‌شوند — حتی بدون آپدیت دستی تنظیمات.

### ۳) WARP پیش‌فرض خاموش شد
- WARP بدون حساب واقعی وصل نمی‌شود؛ قبلاً پیش‌فرض «روشن» بود و کانفیگ WARP مرده تولید می‌کرد. حالا پیش‌فرض خاموش است و فقط در صورت فعال‌کردن دستی تولید می‌شود.

## تست End-to-End (روی ورکر زندهٔ Cloudflare)
یک ورکر واقعی روی اکانت ساختیم، کانفیگ تولید کردیم و با کلاینت WS واقعی تست کردیم:
- VLESS → upgrade **101** + پاسخ `[0,0]` + **HTTP 200** واقعی از مقصد ✔
- Trojan → upgrade **101** + **HTTP 200** واقعی از مقصد ✔

## وضعیت انتشار
- GitHub: نسخهٔ 0.6.1 push شد (`96c9a69`).
- ربات `nika-launcher` با باندل جدید دیپلوی شد (KV و سکرت‌ها دست‌نخورده).
- کرون ربات (هر ۱۰ دقیقه) نسخهٔ جدید را می‌بیند و به همهٔ کاربران اعلام می‌کند؛ کاربران از داخل پنل یا ربات «بروزرسانی» را می‌زنند تا پنل‌شان 0.6.1 بگیرد.
